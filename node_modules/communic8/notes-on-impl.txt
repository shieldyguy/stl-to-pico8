reader:
- can have multiple listeners

readerPollingListener:
- only one such listener exists


polling master -> reader ->
                         -> -> many potentially
                         ->
